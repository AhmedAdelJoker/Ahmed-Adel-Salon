/**
 * @deprecated Merged into `../owner/Expenses`.
 * The `/expenses` route renders the Owner Expenses implementation, which
 * already serves all roles (OWNER/ADMIN/MANAGER/CASHIER/ACCOUNTANT).
 * This legacy simplified copy was never imported by the router.
 * Kept as a thin re-export so any stale import keeps working and renders
 * the single canonical implementation.
 */
export { default } from "@/pages/owner/Expenses";
